// Discrete joystick target positions (D-pad)
#pragma once

// Positions are relative to platform coordinate frame (defined in main.cpp)
extern float T_CENTER[3];     // Home position
extern float T_FORWARD[3];    // D-pad UP
extern float T_BACK[3];       // D-pad DOWN
extern float T_RIGHT[3];      // D-pad RIGHT
extern float T_LEFT[3];       // D-pad LEFT
